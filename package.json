{
  "name": "web-biblia",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "data:optimize": "tsx scripts/optimize-data.ts",
    "build": "pnpm data:optimize && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "cf:deploy": "astro build && wrangler pages deploy dist",
    "cf:preview": "astro build && wrangler pages deploy dist --branch preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/tailwind": "^6.0.2",
    "@tailwindcss/vite": "^4.1.17",
    "astro": "^5.17.2",
    "fuse.js": "^7.1.0",
    "tailwindcss": "^4.1.17",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "wrangler": "^4.65.0"
  }
}
